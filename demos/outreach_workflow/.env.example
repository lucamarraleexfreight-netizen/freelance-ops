# Copy to .env and fill in ONLY if you set drafting.use_llm: true in config.yaml.
# The default templated drafter needs none of this.
#
# On Windows (PowerShell), load it for a session with:
#   Get-Content .env | ForEach-Object { if ($_ -match '^\s*([^#=]+)=(.*)$') { [Environment]::SetEnvironmentVariable($matches[1].Trim(), $matches[2].Trim()) } }

ANTHROPIC_API_KEY=sk-ant-your-key-here
